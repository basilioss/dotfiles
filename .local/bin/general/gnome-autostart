#!/bin/sh

# Decrease key repeat delay to 300ms and increase key repeat rate to 50 per second.
# Faster j k scrolling in vim.
xset r rate 300 50

# xrdb -merge "$HOME/.config/x11/xresources"
# sxhkd &
# unclutter &
# playerctld daemon &
# udiskie &
notify-syncthing &
notify-battery &
git-backup &
